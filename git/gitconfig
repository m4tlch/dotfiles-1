[user]
    name = David Jegat
    email = david.jegat@gmail.com
[color]
    ui = true
[core]
    excludesfile = ~/.gitignore
[alias]
    st = status
    ci = commit
    br = branch
    co = checkout
    df = diff
    lg = log -p
    all = add -A
    amend = commit --amend
    aci = !git add -A && git commit -m

[merge]
    tool = vimdiff

[url "git@github.com:"]
    insteadOf = gh:
[push]
    default = current
[http]
