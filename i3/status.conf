order += "tztime local"
order += "battery 0"

battery 0 {
    format = "%status %percentage - %remaining"
    low_threshold = 30
    threshold_type = time
    integer_battery_capacity = true
}

tztime local {
    format = "%Y-%m-%d %H:%M:%S"
}
